# Credentials are auto-populated by the pairing flow on first `npm start`.
# You don't need to fill these manually.
# PRIVOS_URL=
# CLIENT_ID=
# CLIENT_SECRET=

# Optional: UI dev server port (default 10002). Only needed for local development.
# PORT=10002
